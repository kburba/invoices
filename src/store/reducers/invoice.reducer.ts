const initialState = {
  invoices: [],
};

const invoiceReducer = (state = initialState, action: any) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default invoiceReducer;
